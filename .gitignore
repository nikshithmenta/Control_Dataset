# downloaded source data / caches (regenerated by generate.py)
control-dataset/.cache/
__pycache__/
*.pyc

# the source paper PDF (linked, not redistributed)
*.pdf

# LaTeX build artifacts
report/*.aux
report/*.log
report/*.out
report/*.pdf
